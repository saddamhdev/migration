Good one 👍
**Flyway file naming is strict** — if you get this wrong, Flyway will **ignore** the file.

Let’s lock it in properly.

---

## The STANDARD Flyway filename format

```
V<version>__<description>.sql
```

### Example

```
V1__init_schema.sql
V2__add_account_table.sql
V3__add_interest_rate.sql
```

---

## Break it down (very important)

### 1️⃣ `V`  → Versioned migration

* **Must be capital `V`**
* Tells Flyway: *this is a versioned migration*

❌ `v1__init.sql` → wrong
✅ `V1__init.sql` → correct

---

### 2️⃣ `<version>` → Ordering (THIS decides execution order)

Valid:

```
1
2
3
1.1
1.2
2.0
```

Examples:

```
V1__init.sql
V2__add_users.sql
V3__add_index.sql
```

Flyway runs them **in version order**, not file-name order.

⚠️ Recommendation:

> Use **simple integers**: `1, 2, 3, 4`

---

### 3️⃣ `__` → TWO underscores (mandatory)

❌ One underscore → wrong
❌ Three underscores → wrong
✅ Exactly **two underscores**

---

### 4️⃣ `<description>` → Human readable

* Any text
* Use lowercase + `_`
* For your understanding only

Examples:

```
init_schema
add_status_column
fix_interest_rate_precision
```

---

### 5️⃣ `.sql` → File extension

* SQL migrations must end with `.sql`

---

## Correct examples (copy-paste safe)

```
V1__init_schema.sql
V2__seed_default_data.sql
V3__add_interest_rate.sql
V4__backfill_interest_rate.sql
V5__make_interest_rate_not_null.sql
```

---

## ❌ Common mistakes (very common)

| Mistake               | Why it fails                 |
| --------------------- | ---------------------------- |
| `V1_init.sql`         | Missing double underscore    |
| `v1__init.sql`        | Lowercase `v`                |
| `V01__init.sql`       | Confusing ordering           |
| `V1__Init.sql`        | Case-sensitive checksum risk |
| `V1__init_schema.SQL` | Wrong extension              |

---

## Where files must live

```
src/main/resources/db/migration/
```

Flyway scans **only this folder** by default.

---

## Baseline case (important)

If you used **baseline-version = 1**:

* Do NOT create `V1__*.sql`
* Start from:

```
V2__new_change.sql
```

Because Flyway already considers version `1` as applied.

---

## Special types (FYI – optional)

| Prefix | Purpose                        |
| ------ | ------------------------------ |
| `V`    | Versioned (most used)          |
| `R`    | Repeatable (views, procedures) |

Example:

```
R__refresh_views.sql
```

Runs when content changes.

---

## Interview-ready one-liner 🎯

> Flyway migration files follow the format `V<version>__<description>.sql`, where the version controls execution order and the double underscore separates metadata from description.

---

## Memory trick 🧠

> **V + number + `__` + meaning + `.sql`**

That’s it.

If you want next:

* how to choose version numbers in teams
* repeatable migrations (`R__`)
* what happens if two devs create same version
* naming strategy for microservices

Just say 👍
